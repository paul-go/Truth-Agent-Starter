{
	"name": "truth-agent-starter",
	"version": "1.0.0",
	"description": "A starter project for developing a Truth agent",
	"main": "build/agent.truth.js",
	"license": "MIT",
	"scripts": {
		"compile": "tsc && rollup -c",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"@types/react": "^16.8.4",
		"rollup-plugin-sourcemaps": "^0.4.2",
		"truth-agent": "^0.2.10",
		"truth-editor": "^0.0.2"
	}
}
